<h1>Editing Book</h1>

     <% if @book.errors.any? %>
     <div class ="error_explanation">
  <h2><%= @book.errors.count %>errors prohibited this book from being saved:</h2>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>

  </ul>
  </div>
<% end %>

<%= form_with model: @book do |f| %>
  <div class= "field">
    <div class="<%="field_with_errors" if @book.errors.full_messages.include?("Title can't be blank") %>">
      <label>Title</label><br>
    </div>
    <div class="<%="field_with_errors" if @book.errors.full_messages.include?("Title can't be blank") %>">

      <%= f.text_field :title %>
    </div>
      </div>

    <div class="field">
       <div class="<%="field_with_errors" if @book.errors.full_messages.include?("Body can't be blank") %>">
      <label>Body</label>
      </div>

         <div class="<%="field_with_errors" if @book.errors.full_messages.include?("Body can't be blank") %>">
        <%= f.text_area :body %></div>
        </div>

  <%= f.submit 'Update Book' %>

<% end %>

    <td><%= link_to 'Show',book_path(@book.id) %></td>
      |
     <td><%= link_to 'Back', books_path %></td>
